import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        width: '100%',
    },
    separator: {
        flex: 1,
        backgroundColor: '#ccc',
    }
});
